"""Textual full-screen UI for the ai-api-unified sample app.

Non-UI logic lives in standalone modules the screens import: catalog, state,
envfile, onboarding, middleware_profile, obs, samples, promptgen, audio,
structured_schemas, and voice_util.
"""
